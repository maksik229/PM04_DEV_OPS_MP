# Практическое занятие 8  
**Создание и сохранение файлов**  

---

## 📌 Основные задания (обязательные)

Выполните в **одном проекте** следующие задачи:

1. **Разработать приложение, которое создаёт текстовый файл и записывает в него:**  
   - произвольный текст,
   - заданное количество случайных чисел,
   - массив значений, полученных вычислением математической функции (например, sin или cos).

2. **Разработать приложение, которое создает текстовый файл заданной структуры ( таблица) для осциллографа EWB**
   <img width="903" height="628" alt="image" src="https://github.com/user-attachments/assets/8dbf25e5-714a-496b-8e74-c70e3f2eb2b5" />
Приложение, создающее текстовый файл в виде таблицы для осциллографа EWB. Файл должен содержать два столбца: Time (временные метки) и V1 (напряжение логических уровней: 0 В — низкий уровень, 5 В — высокий).
- Пользователь задаёт:
   - частоту следования временных меток (шаг по времени),
   - количество бит (длину последовательности), способ формирования количества бит  — вручную или случайным образом.
   - длительность передачи
- Приложение генерирует таблицу в соответствии с этими параметрами и сохраняет её в текстовый файл.


3. **Разработать приложение, которое создаёт файлы в форматах Microsoft Word (.docx) и Microsoft Excel (.xlsx), содержащие таблицу с двумя столбцами: Номер измерения и Результат (целое число от 0 до 100).**  
   - Пользователь задаёт:
     - общее количество строк (объём выборки),
     - способ заполнения второго столбца — вручную или автоматически (с генерацией случайных целых значений в указанном диапазоне).
   - Приложение формирует таблицу в соответствии с заданными параметрами и сохраняет её одновременно в виде документа Word (с чётко оформленной таблицей) и файла Excel (с данными, пригодными для дальнейшей обработки или построения диаграмм).


4. **Разработать приложение для работы со звуком**
   - Приложение должно:
- воспроизводит и записывает аудио в файл;
- позволяет слушать онлайн-радио (например, с использованием библиотеки BASS);
- генерирует звуковые сигналы с регулируемой частотой, амплитудой, формой волны (синус, меандр, пила и др.) и возможностью формирования пакетов импульсов;
- визуализирует звук в реальном времени в виде осциллограммы и спектра (спектроанализатор).


5. **Разработать приложение, способное передавать данные через различные каналы связи**  
   - отправка по электронной почте,
   - в виде SMS-сообщений,
   - через популярные мессенджеры, 
   - мобильные приложения (например, с использованием push-уведомлений или API мобильных платформ).

> 💡 Дизайн должен быть **аккуратным**: выравнивание, подписи к компонентам, отступы.

---

## 📁 Структура проекта

- Форма должна быть **понятной и удобной** — не «куча компонентов в углу».
- Используйте `TPanel`, `TGroupBox`, подписи (`Caption`) для группировки.

---

## 📤 Сдача работы

1. Загрузите **весь проект** в репозиторий:
   - `.lpi`, `.lpr`, `.lfm`, `.pas` и другие файлы проекта.
2. Убедитесь, что проект **компилируется и запускается**.
3. Сделайте **несколько осмысленных коммитов**.

> ⚠️ Последний коммит — до дедлайна.  
> Работы без индивидуального имени — **0 баллов**.

---

## 🛑 Запрещено

- Использовать чужие проекты без изменений.
- Копировать код из интернета без понимания.
- Использовать изображения с котиками, мемами, 52 логотипами.

---

Удачи! Помните: цель — **научиться управлять компонентами**, а не просто «сдать».
